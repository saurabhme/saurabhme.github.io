---
title: 'Math and Code Test'
description: 'Testing LaTeX and Code highlighting in Astro'
pubDate: '2025-11-21'
heroImage: '/blog-placeholder-1.jpg'
---

## Introduction

This is a test post to verify that **LaTeX** and **Code Highlighting** are working correctly.

## Math Equation

Here is a complex equation using LaTeX:

$$
\mathcal{L}_{ELBO} = \mathbb{E}_{q(z|x)}[\log p(x|z)] - D_{KL}(q(z|x) || p(z))
$$

And an inline equation: $e^{i\pi} + 1 = 0$.

## Code Block

Here is some Python code:

```python
import numpy as np

def relu(x):
    return np.maximum(0, x)

x = np.array([-1, 0, 1])
print(relu(x))
```

## Conclusion

If you see the math rendered beautifully and the code highlighted, then the system is working!
